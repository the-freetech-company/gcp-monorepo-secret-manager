# gcp-monorepo-secret-manager

This package was renamed to [`monorepo-secret-manager`](https://www.npmjs.com/package/monorepo-secret-manager).

```bash
npm install monorepo-secret-manager
```

This release re-exports the new package so existing imports keep working while you migrate.
